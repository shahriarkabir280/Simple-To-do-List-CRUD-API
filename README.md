# Simple-To-do-List-CRUD-API
A simple to-do list CRUD API built with Node.js and Express — create, read, update, and delete tasks with full REST semantics, in-memory storage, and interactive Swagger UI docs.
